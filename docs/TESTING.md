# Part 1 Testing

## ERD Testing

The ERD was checked to ensure that all required entities, attributes, primary keys and foreign keys are included and that the relationships match the database design.

## API Plan Testing

The API endpoint plan was reviewed to ensure that the required endpoints, HTTP methods, roles, request bodies and expected response codes are documented.

## SQL Testing

The SQL script was executed successfully in SQL Server Management Studio. The script creates the RaceDay database, tables, relationships, constraints and sample data.

## GitHub Actions Testing

GitHub Actions was used to validate that the required Part 1 documents are present in the `docs` folder. The workflow completed successfully with a green check.

## Result

The Part 1 planning documents and database script were checked before submission.